// ═══════════════════════════════════════════════════════════
// НАСТРОЙКИ БОТА — редактируй под себя, ничего кроме этого файла
// трогать не нужно
// ═══════════════════════════════════════════════════════════

module.exports = {
  // ── ВАЛЮТА ──
  // Впиши код своего кастомного эмодзи пива. Как получить код:
  // напиши в любом текстовом канале Discord "\:cupbeer:" (с обратным слэшем перед
  // двоеточием) — бот покажет код вида <:cupbeer:1234567890123456789>, скопируй
  // его целиком сюда вместо плейсхолдера ниже.
  CURRENCY_EMOJI: "<:cupbeer:1543735915623612506>",

  // ── ЗАРАБОТОК В ЧАТЕ ──
  CHAT_EARN_MIN: 0,        // минимум пива за сообщение
  CHAT_EARN_MAX: 1,       // максимум пива за сообщение
  CHAT_COOLDOWN_SEC: 30,   // раз в сколько секунд можно заработать за сообщение

  // ── ЗАРАБОТОК В ВОЙСЕ ──
  VOICE_EARN_AMOUNT: 7,       // сколько пива начисляется за интервал
  VOICE_INTERVAL_MINUTES: 5,   // раз в сколько минут начисляется
  VOICE_REQUIRE_NOT_ALONE: true, // true = не платить, если человек один в канале (без учёта ботов)
  VOICE_IGNORE_AFK: true,      // не платить, если человек в AFK-канале
  VOICE_IGNORE_MUTED_DEAFENED: true, // true = не платить, если человек в мьюте/дефе

  // ── ЕЖЕДНЕВНЫЙ БОНУС ──
  DAILY_AMOUNT: 15,

  // ── ИНВАЙТ-ТРЕКИНГ: ПОРОГИ И РОЛИ ──
  // count — сколько живых (не вышедших раньше LEAVE_PENALTY_DAYS) рефералов нужно
  // roleId — ID роли на сервере (см. README как достать)
  // label — просто для логов/сообщений
  // coinReward — сколько пива начисляется вместе с ролью за достижение порога.
  // При потере роли (человек вышел раньше недели) это же количество пива
  // списывается обратно — баланс может уйти в минус, если пиво уже потрачено.
  INVITE_THRESHOLDS: [
    { count: 1, roleId: "1543457535707713637", label: "Санитар Утопии", coinReward: 50 },
    { count: 3, roleId: "1543458447373246464", label: "Кадровик барака", coinReward: 200 },
    { count: 7, roleId: "1543459062832955463", label: "Отец-Основатель", coinReward: 500 },
  ],

  // если приглашённый выходит с сервера раньше этого срока — инвайт не засчитывается / снимается
  LEAVE_PENALTY_DAYS: 7,

  // ── МАГАЗИН (покупка ролей за пиво) ──
  // Магазин показывается с кнопками — категории раскрываются в список товаров.
  // roleId — ID роли, которая выдаётся при покупке
  SHOP_CATEGORIES: [
    {
      id: "roles",
      name: "Роль",
      emoji: "🧌",
      items: [
        { id: "brat", name: "Брат", price: 1200, roleId: "1420292146040144024", emoji: "🐦‍🔥" },
        { id: "storogila", name: "Сторожила", price: 1000, roleId: "1420291717302587442", emoji: "⚠️" },
      ],
    },
    // можешь добавить ещё категории по этому же образцу, например:
    // {
    //   id: "misc",
    //   name: "Разное",
    //   emoji: "🎁",
    //   items: [
    //     { id: "brat", name: "Брат", price: 1200, roleId: "ROLE_ID_BRAT", emoji: "🍺" },
    //   ],
    // },
  ],

  // ── КАНАЛ ДЛЯ ЛОГОВ (необязательно) ──
  // ID текстового канала, куда бот будет писать "X получил роль за инвайты" и т.п.
  // Оставь пустой строкой "" если не нужно
  LOG_CHANNEL_ID: "1543736755830784150",
};
